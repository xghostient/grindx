{
  "id": "mst-kruskal-algo",
  "name": "Minimum Spanning Tree (Kruskal's Algorithm)",
  "difficulty": "Medium",
  "description": "Given a weighted undirected graph, find the sum of weights of the minimum spanning tree using Kruskal's algorithm. Sort edges by weight and use Union-Find to avoid cycles.",
  "examples": [
    {
      "input": "V=5, edges=[[0,1,2],[0,3,6],[1,2,3],[1,3,8],[1,4,5],[2,4,7]]",
      "output": "16"
    },
    {
      "input": "V=3, edges=[[0,1,5],[1,2,3],[0,2,1]]",
      "output": "4"
    }
  ],
  "constraints": "2 <= V <= 10^5. V-1 <= E <= V*(V-1)/2. 1 <= weight <= 10^5.",
  "python_template": "# Minimum Spanning Tree (Kruskal's Algorithm)\n\nfrom typing import List\n\ndef kruskalMST(V: int, edges: List[List[int]]) -> int:\n    \"\"\"edges[i] = [u, v, w]. Return MST weight sum.\"\"\"\n    pass\n",
  "go_template": "// Minimum Spanning Tree (Kruskal's Algorithm)\n\npackage main\n\n\nfunc kruskalMST(V int, edges [][]int) int {\n\treturn 0\n}\n",
  "cpp_template": "// Minimum Spanning Tree (Kruskal's Algorithm)\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint kruskalMST(int V, vector<vector<int>>& edges) {\n    return 0;\n}\n",
  "java_template": "// Minimum Spanning Tree (Kruskal's Algorithm)\n\nclass Solution {\n    public int kruskalMST(int V, int[][] edges) {\n        return 0;\n    }\n}\n",
  "js_template": "// Minimum Spanning Tree (Kruskal's Algorithm)\n\nfunction kruskalMST(V, edges) {\n\n}\n",
  "performance": {
    "target_time": "O(E log E)",
    "accepted_time": [
      "O(E log E)"
    ],
    "rejected_time": [
      "O(E^2)"
    ],
    "target_space": "O(V)",
    "stress_intent": "exercise edge sorting and cycle skipping for MST sum; performance enforcement is deferred",
    "reference_solution_required": true,
    "enforcement": "best-effort-local"
  }
}
