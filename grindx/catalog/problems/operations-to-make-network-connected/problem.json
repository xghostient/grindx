{
  "id": "operations-to-make-network-connected",
  "name": "Number of Operations to Make Network Connected",
  "difficulty": "Medium",
  "description": "Given n computers and a list of connections, find the minimum number of cable moves needed to connect all computers. If impossible, return -1. You need at least n-1 cables total.",
  "examples": [
    {
      "input": "n = 4, connections = [[0,1],[0,2],[1,2]]",
      "output": "1"
    },
    {
      "input": "n = 6, connections = [[0,1],[0,2],[0,3],[1,2],[1,3]]",
      "output": "2"
    },
    {
      "input": "n = 6, connections = [[0,1],[0,2],[0,3],[1,2]]",
      "output": "-1 (not enough cables)"
    }
  ],
  "constraints": "1 <= n <= 10^5. 1 <= connections.length <= min(n*(n-1)/2, 10^5).",
  "python_template": "# Number of Operations to Make Network Connected\n\nfrom typing import List\n\ndef makeConnected(n: int, connections: List[List[int]]) -> int:\n    pass\n",
  "go_template": "// Number of Operations to Make Network Connected\n\npackage main\n\nfunc makeConnected(n int, connections [][]int) int {\n\treturn 0\n}\n",
  "cpp_template": "// Number of Operations to Make Network Connected\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint makeConnected(int n, vector<vector<int>>& connections) {\n    return 0;\n}\n",
  "java_template": "// Number of Operations to Make Network Connected\n\nclass Solution {\n    public int makeConnected(int n, int[][] connections) {\n        return 0;\n    }\n}\n",
  "js_template": "// Number of Operations to Make Network Connected\n\nfunction makeConnected(n, connections) {\n\n}\n",
  "performance": {
    "target_time": "O(V + E)",
    "accepted_time": [
      "O(V + E)",
      "O(E * alpha(V))"
    ],
    "rejected_time": [
      "O(V * E)"
    ],
    "target_space": "O(V)",
    "stress_intent": "kill repeated whole-graph reachability scans on large sparse networks while still accepting one-pass BFS/DFS and DSU solutions",
    "reference_solution_required": true,
    "enforcement": "strict-local"
  }
}
