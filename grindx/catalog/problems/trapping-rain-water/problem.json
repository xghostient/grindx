{
  "id": "trapping-rain-water",
  "name": "Trapping Rain Water",
  "difficulty": "Hard",
  "description": "Given n non-negative integers representing an elevation map where the width of each bar is 1, compute how much water it can trap after raining.",
  "examples": [
    {
      "input": "height = [0,1,0,2,1,0,1,3,2,1,2,1]",
      "output": "6"
    },
    {
      "input": "height = [4,2,0,3,2,5]",
      "output": "9"
    }
  ],
  "constraints": "n == height.length. 1 <= n <= 2 * 10^4. 0 <= height[i] <= 10^5.",
  "python_template": "# Trapping Rain Water\n\nfrom typing import List\n\ndef trap(height: List[int]) -> int:\n    pass\n",
  "go_template": "// Trapping Rain Water\n\npackage main\n\nfunc trap(height []int) int {\n\treturn 0\n}\n",
  "cpp_template": "// Trapping Rain Water\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint trap(vector<int>& height) {\n    return 0;\n}\n",
  "java_template": "// Trapping Rain Water\n\nclass Solution {\n    public int trap(int[] height) {\n        return 0;\n    }\n}\n",
  "js_template": "// Trapping Rain Water\n\nfunction trap(height) {\n\n}\n",
  "performance": {
    "target_time": "O(n)",
    "accepted_time": [
      "O(n)"
    ],
    "rejected_time": [
      "O(n^2)"
    ],
    "target_space": "O(1)",
    "stress_intent": "exercise 20k height arrays that punish quadratic left-right max recomputation",
    "reference_solution_required": true,
    "enforcement": "best-effort-local"
  }
}
