{
  "id": "sqrtx",
  "name": "Sqrt(x)",
  "difficulty": "Easy",
  "description": "Given a non-negative integer x, return the square root of x rounded down to the nearest integer. Do not use any built-in exponent function or operator.",
  "examples": [
    {
      "input": "x = 4",
      "output": "2"
    },
    {
      "input": "x = 8",
      "output": "2 (sqrt(8) = 2.828..., rounded down)"
    }
  ],
  "constraints": "0 <= x <= 2^31 - 1.",
  "python_template": "# Sqrt(x)\n\ndef mySqrt(x: int) -> int:\n    pass\n",
  "go_template": "// Sqrt(x)\n\npackage main\n\nfunc mySqrt(x int) int {\n\treturn 0\n}\n",
  "cpp_template": "// Sqrt(x)\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint mySqrt(int x) {\n    return 0;\n}\n",
  "java_template": "// Sqrt(x)\n\nclass Solution {\n    public int mySqrt(int x) {\n        return 0;\n    }\n}\n",
  "js_template": "// Sqrt(x)\n\nfunction mySqrt(x) {\n\n}\n",
  "performance": {
    "target_time": "O(log n)",
    "accepted_time": [
      "O(log n)"
    ],
    "rejected_time": [
      "O(sqrt(n))"
    ],
    "target_space": "O(1)",
    "stress_intent": "exercise large integers that punish linear or sqrt-step search",
    "reference_solution_required": true,
    "enforcement": "best-effort-local"
  }
}
