{
  "id": "subsets",
  "name": "Subsets",
  "difficulty": "Medium",
  "description": "Given an integer array nums of unique elements, return all possible subsets (the power set). The solution set must not contain duplicate subsets. Return the subsets in any order.",
  "examples": [
    {
      "input": "nums = [1,2,3]",
      "output": "[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]"
    },
    {
      "input": "nums = [0]",
      "output": "[[],[0]]"
    }
  ],
  "constraints": "1 <= nums.length <= 10. -10 <= nums[i] <= 10. All elements of nums are unique.",
  "python_template": "# Subsets\n\nfrom typing import List\n\ndef subsets(nums: List[int]) -> List[List[int]]:\n    pass\n",
  "go_template": "// Subsets\n\npackage main\n\nfunc subsets(nums []int) [][]int {\n\treturn [][]int{}\n}\n",
  "cpp_template": "// Subsets\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nvector<vector<int>> subsets(vector<int>& nums) {\n    return {};\n}\n",
  "java_template": "// Subsets\n\nclass Solution {\n    public int[][] subsets(int[] nums) {\n        return new int[][]{};\n    }\n}\n",
  "js_template": "// Subsets\n\nfunction subsets(nums) {\n\n}\n",
  "performance": {
    "target_time": "O(n * 2^n)",
    "accepted_time": [
      "O(n * 2^n)",
      "O(n^2 * 2^n)"
    ],
    "rejected_time": [
      "O(n^2 * 2^n)"
    ],
    "target_space": "O(n)",
    "stress_intent": "exercise 10-element arrays that confirm all 2^n subsets generated",
    "reference_solution_required": true,
    "enforcement": "best-effort-local"
  }
}
