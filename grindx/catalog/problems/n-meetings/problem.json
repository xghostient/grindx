{
  "id": "n-meetings",
  "name": "N Meetings in One Room",
  "difficulty": "Easy",
  "description": "There is one meeting room. You are given n meetings with their start and end times. Find the maximum number of meetings that can be accommodated in the room. Only one meeting can be held at a time. A meeting can start at the same time another ends. Greedy: sort by end time and greedily pick non-overlapping meetings.",
  "examples": [
    {
      "input": "start = [1, 3, 0, 5, 8, 5], end = [2, 4, 6, 7, 9, 9]",
      "output": "4 (meetings: [1,2], [3,4], [5,7], [8,9])"
    },
    {
      "input": "start = [10, 12, 20], end = [20, 25, 30]",
      "output": "1"
    },
    {
      "input": "start = [1, 2], end = [2, 3]",
      "output": "2"
    }
  ],
  "constraints": "1 <= n <= 10^5. 0 <= start[i] < end[i] <= 10^5.",
  "python_template": "# N Meetings in One Room\n\nfrom typing import List\n\ndef maxMeetings(start: List[int], end: List[int]) -> int:\n    pass\n",
  "go_template": "// N Meetings in One Room\n\npackage main\n\nfunc maxMeetings(start, end []int) int {\n    return 0\n}\n",
  "cpp_template": "// N Meetings in One Room\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint maxMeetings(vector<int>& start, vector<int>& end) {\n    return 0;\n}\n",
  "java_template": "// N Meetings in One Room\n\nclass Solution {\n    public int maxMeetings(int[] start, int[] end) {\n        return 0;\n    }\n}\n",
  "js_template": "// N Meetings in One Room\n\nfunction maxMeetings(start, end) {\n\n}\n",
  "performance": {
    "target_time": "O(n log n)",
    "accepted_time": [
      "O(n log n)"
    ],
    "rejected_time": [
      "O(n^2)"
    ],
    "target_space": "O(n)",
    "stress_intent": "exercise large meeting sets that punish non-sorted selection",
    "reference_solution_required": true,
    "enforcement": "strict-local"
  }
}
