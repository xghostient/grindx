{
  "id": "lcm-gcd",
  "name": "LCM and GCD",
  "difficulty": "Easy",
  "description": "Given two positive integers a and b, compute both their Greatest Common Divisor (GCD) and Least Common Multiple (LCM). Use the Euclidean algorithm for GCD: gcd(a, b) = gcd(b, a % b). LCM can be derived as: lcm(a, b) = (a * b) / gcd(a, b).",
  "examples": [
    {
      "input": "a = 12, b = 18",
      "output": "[6, 36] (GCD=6, LCM=36)"
    },
    {
      "input": "a = 5, b = 7",
      "output": "[1, 35]"
    },
    {
      "input": "a = 100, b = 25",
      "output": "[25, 100]"
    }
  ],
  "constraints": "1 <= a, b <= 10^9. The LCM can exceed 32-bit signed range, so use 64-bit arithmetic for the output.",
  "python_template": "# LCM and GCD\n\nfrom typing import List\n\ndef lcmAndGcd(a: int, b: int) -> List[int]:\n    \"\"\"Returns [gcd, lcm]\"\"\"\n    pass\n",
  "go_template": "// LCM and GCD\n\npackage main\n\nfunc lcmAndGcd(a, b int) []int64 {\n\treturn []int64{}\n}\n",
  "cpp_template": "// LCM and GCD\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nvector<long long> lcmAndGcd(int a, int b) {\n    return {};\n}\n",
  "java_template": "// LCM and GCD\n\nclass Solution {\n    public long[] lcmAndGcd(int a, int b) {\n        return new long[]{};\n    }\n}\n",
  "js_template": "// LCM and GCD\n\nfunction lcmAndGcd(a, b) {\n\n}\n",
  "performance": {
    "target_time": "O(log(min(a, b)))",
    "accepted_time": [
      "O(log(min(a, b)))"
    ],
    "rejected_time": [
      "O(min(a, b))"
    ],
    "target_space": "O(1)",
    "stress_intent": "exercise large values that punish subtraction-based GCD",
    "reference_solution_required": true,
    "enforcement": "best-effort-local"
  }
}
