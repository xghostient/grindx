{
  "id": "shortest-path-ug",
  "name": "Shortest Path in Unweighted Graph",
  "difficulty": "Easy",
  "description": "Given an unweighted undirected graph with V vertices and E edges, find the shortest path from source to all other vertices using BFS. If unreachable, distance is -1.",
  "examples": [
    {
      "input": "V=4, edges=[[0,1],[0,2],[1,3],[2,3]], src=0",
      "output": "[0,1,1,2]"
    },
    {
      "input": "V=3, edges=[[0,1]], src=0",
      "output": "[0,1,-1]"
    }
  ],
  "constraints": "1 <= V <= 10^4. 0 <= E <= V*(V-1)/2.",
  "python_template": "# Shortest Path in Unweighted Graph\n\nfrom typing import List\nfrom collections import deque\n\ndef shortestPath(V: int, adj: List[List[int]], src: int) -> List[int]:\n    pass\n",
  "go_template": "// Shortest Path in Unweighted Graph\n\npackage main\n\nfunc shortestPath(V int, adj [][]int, src int) []int {\n\treturn []int{}\n}\n",
  "cpp_template": "// Shortest Path in Unweighted Graph\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nvector<int> shortestPath(int V, vector<vector<int>>& adj, int src) {\n    return {};\n}\n",
  "java_template": "// Shortest Path in Unweighted Graph\n\nclass Solution {\n    public int[] shortestPath(int V, int[][] adj, int src) {\n        return new int[]{};\n    }\n}\n",
  "js_template": "// Shortest Path in Unweighted Graph\n\nfunction shortestPath(V, adj, src) {\n\n}\n",
  "performance": {
    "target_time": "O(V + E)",
    "accepted_time": [
      "O(V + E)"
    ],
    "rejected_time": [
      "O(V * (V + E))"
    ],
    "target_space": "O(V)",
    "stress_intent": "exercise BFS shortest distances from every small source vertex; performance enforcement is deferred",
    "reference_solution_required": true,
    "enforcement": "best-effort-local"
  }
}
